// Public API URL only. No database credentials, Brevo keys or Diagnostic signing key.
// Point to the approved staging API for review; production activation requires release approval.
window.CONSULTING_CONFIG = Object.freeze({
  apiBaseUrl: 'https://crstays-ppa.onrender.com'
});
